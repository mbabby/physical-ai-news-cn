name: Daily physical AI digest

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "events/companies.json"
      - ".github/workflows/daily-digest.yml"
  schedule:
    # 提前探测：00:00 UTC = 北京时间 08:00。GitHub cron 可能排队，
    # 多个幂等触发点提高当天按时产出的概率；已有机器人日报时会立即跳过。
    - cron: "0 0 * * *"
    # 主任务：00:30 UTC = 北京时间 08:30
    - cron: "30 0 * * *"
    # 兜底任务：01:15 UTC = 北京时间 09:15。仅当主任务没有产出当天日报时运行。
    - cron: "15 1 * * *"
  workflow_dispatch:
    inputs:
      hours:
        description: "回填窗口（首次初始化填 720；日更留空）"
        required: false
        default: ""

permissions:
  contents: write
  issues: read

# 同一时间只允许一轮日报写入 main；避免手动触发与定时任务彼此抢占提交。
concurrency:
  group: daily-physical-ai-digest
  cancel-in-progress: false

jobs:
  generate:
    # Hard ceiling for the complete pipeline. The generator has a smaller
    # ceiling below so cleanup/status steps still have time to run.
    timeout-minutes: 22
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Sync latest main before generation
        # A queued manual/push run can start just after the preceding digest
        # commits the same daily file. Generate from that newest commit, not
        # from the older triggering SHA, so the later rebase cannot conflict
        # on daily/YYYY-MM-DD.{md,json}.
        run: git pull --ff-only origin main
      - name: Decide whether the fallback run is needed
        id: fallback
        shell: bash
        run: |
          echo "run=true" >> "$GITHUB_OUTPUT"
          # Every scheduled trigger is idempotent. The earliest trigger that
          # reaches a runner generates the digest; later delayed triggers skip.
          if [[ -z "${{ github.event.schedule }}" ]]; then
            exit 0
          fi
          digest_date="$(TZ=Asia/Shanghai date +%F)"
          if git log --format='%an' --since="${digest_date} 00:00:00 +0800" -- "daily/${digest_date}.md" | grep -qx "physical-ai-news-bot"; then
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "A digest was already committed for ${digest_date}; this scheduled retry is skipped."
          fi
      - uses: actions/setup-node@v5
        if: steps.fallback.outputs.run == 'true'
        with:
          node-version: 24
          # setup-node v5 auto-detects package managers before corepack runs;
          # disabling its cache avoids a false "pnpm not found" failure.
          package-manager-cache: false
      - run: corepack enable
        if: steps.fallback.outputs.run == 'true'
      - run: pnpm install --frozen-lockfile --config.strict-dep-builds=false
        if: steps.fallback.outputs.run == 'true'
      - run: pnpm run check
        if: steps.fallback.outputs.run == 'true'
      - run: pnpm test
        if: steps.fallback.outputs.run == 'true'
      - name: Generate digest with a bounded deadline
        id: generate_digest
        shell: bash
        run: |
          set +e
          timeout --signal=TERM --kill-after=30s 15m pnpm start -- --hours "${DIGEST_HOURS:-24}"
          exit_code=$?
          set -e
          if [[ "$exit_code" -eq 124 || "$exit_code" -eq 137 ]]; then
            echo "::error::日报生成超过 15 分钟，已终止。本轮不会提交半成品，后续定时触发可安全重试。"
            echo "## 日报生成超时" >> "$GITHUB_STEP_SUMMARY"
            echo "- 生成步骤超过 15 分钟并被终止；事务写入保证公开文件未被部分覆盖。" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi
          exit "$exit_code"
        if: steps.fallback.outputs.run == 'true'
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_MODEL: ${{ vars.LLM_MODEL }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          OPENALEX_API_KEY: ${{ secrets.OPENALEX_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          DIGEST_HOURS: ${{ inputs.hours }}
      - name: Validate generated publication
        if: steps.fallback.outputs.run == 'true'
        run: pnpm run validate:release
      - name: Report generator status
        id: generator_status
        if: steps.fallback.outputs.run == 'true'
        shell: bash
        run: |
          # Bind the report to the date produced by this generator run. A run
          # can start before Beijing midnight and finish after it; recomputing
          # the wall-clock date here would address a daily file that was never
          # generated and skip the otherwise valid commit.
          digest_date="$(jq -er '.date | select(type == "string" and test("^[0-9]{4}-[0-9]{2}-[0-9]{2}$"))' review/run-manifest.json)"
          echo "digest_date=${digest_date}" >> "$GITHUB_OUTPUT"
          echo "## 日报生成状态 · ${digest_date}" >> "$GITHUB_STEP_SUMMARY"
          echo "### 信源" >> "$GITHUB_STEP_SUMMARY"
          jq -r '(.sourceOutcomes // []) | if map(select(.status == "failure")) | length == 0 then "- 成功：本轮已启用信源均完成抓取。" else map(select(.status == "failure") | "- 失败：\(.source)（\(.reason // "未知原因")）")[] end' "daily/${digest_date}.json" >> "$GITHUB_STEP_SUMMARY"
          echo "### LLM 与 OpenAlex" >> "$GITHUB_STEP_SUMMARY"
          jq -r '.runtimeStatus[] | select(.component == "LLM" or .component == "OpenAlex") | "- \(.component)：\(.status)；请求 \(.attempted)，成功 \(.succeeded)，失败 \(.failed)。\(.detail)"' "daily/${digest_date}.json" >> "$GITHUB_STEP_SUMMARY"
          echo "### Watchlist" >> "$GITHUB_STEP_SUMMARY"
          jq -r '.services[] | select(.component == "Watchlist") | "- \(.status)；尝试 \(.attempted)，成功 \(.succeeded)，失败 \(.failed)。\(.detail)"' review/run-manifest.json >> "$GITHUB_STEP_SUMMARY"
          jq -r '"- 公开快照：\(.week) · v\(.snapshotVersion)；前瞻雷达 \(.forwardRadar | length) 张，验证动量 \(.validatedMomentum | length) 张。"' watchlist/current.json >> "$GITHUB_STEP_SUMMARY"
          echo "### 发布事务" >> "$GITHUB_STEP_SUMMARY"
          jq -r '"- 状态：\(.status)；公开产业 \(.quality.publicIndustryItems) 条，研究 \(.quality.publicResearchItems) 条，候选 \(.quality.candidates) 条，失败信源 \(.quality.sourceFailures) 个。"' review/run-manifest.json >> "$GITHUB_STEP_SUMMARY"
          jq -r '"- 连续成功发布：\(.consecutiveSuccessfulPublications) 次；最近成功率：\((.recentSuccessRate * 100 | round))%；健康状态：\(.status)。"' review/pipeline-health.json >> "$GITHUB_STEP_SUMMARY"
      - name: Commit updated digest
        id: commit_digest
        if: steps.fallback.outputs.run == 'true'
        shell: bash
        run: |
          # Keep the generated publication boundary in one executable script.
          # It stages the dashboard, Watchlist change page and feeds together,
          # then fails with exact paths if a future generator adds an output
          # that this release transaction does not yet cover.
          bash scripts/stage-generated-publication.sh
          if ! git diff --cached --quiet; then
            git config user.name "physical-ai-news-bot"
            git config user.email "actions@github.com"
            git commit -m "chore: update daily physical AI digest"
            git pull --rebase origin main
            git push origin HEAD:main
            echo "status=已提交并推送" >> "$GITHUB_OUTPUT"
          else
            echo "status=无内容变化，无需提交" >> "$GITHUB_OUTPUT"
          fi
      - name: Report commit status
        if: always() && steps.fallback.outputs.run == 'true'
        shell: bash
        run: |
          echo "### 页面更新与提交" >> "$GITHUB_STEP_SUMMARY"
          echo "- 生成命令：${{ steps.generate_digest.outcome }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- 发布校验与状态报告：${{ steps.generator_status.outcome }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- 提交步骤：${{ steps.commit_digest.outcome }} · ${{ steps.commit_digest.outputs.status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- 页面更新：日报工作流完成后，由 Pages 的 workflow_run 触发器检出最新 main，避免派发时的引用传播竞态。" >> "$GITHUB_STEP_SUMMARY"
